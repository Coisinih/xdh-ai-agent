# 使用 spring-ai-starter-mcp-server
spring:
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 1.0.0
        stdio: true
        type: SYNC # ??
  main:
    web-application-type: none  # 因为是基于本地的mcp，所以没必要开启web端口
    banner-mode: off  # 关闭banner，减少不必要的日志输出


